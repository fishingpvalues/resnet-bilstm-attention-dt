[project]
name = "resnet-bilstm-attention-dt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "numpy",
    "matplotlib",
    "scikit-learn",
    "torch",
    "holidays",
    "graphviz",
    "torchview",
    "seaborn",
    "pylint>=3.3.6",
    "py2puml>=0.10.0",
    "tqdm>=4.67.1",
]
[tool.setuptools]
package-dir = {"" = "src"}
